📝 AtCoder 習得ログ：C++ 基礎編1. 型の演算と連結C++では、型の組み合わせによって挙動が変わることに注意。組み合わせ挙動備考char + char数値計算文字コード（ASCII）としての足し算になるchar + string連結文字列の末尾に文字が追加されるstring + string連結文字列同士がつながる2. ループの制御 (break)二重ループの注意点: break を書いた 直近のループ（内側） しか抜けない。外側のループまで一気に抜けたい場合は、フラグ（bool）を使うか return を検討する。3. 二次元配列 (vector<vector<T>>)2次元配列のサイズ取得は「親」と「子」の要素数を数えるイメージ。行数: vec.size()列数: vec[0].size() （0番目の行に含まれる要素数）4. 表形式の出力（改行と空白の制御）行末に余計な空白を入れないための定番の実装パターン。C++for (int i = 0; i < N; i++) {
    for (int j = 0; j < N; j++) {
        cout << table[i][j];

        if (j == N - 1) {
            cout << endl; // 行末なら改行
        } else {
            cout << " ";  // それ以外（要素間）は空白
        }
    }
}
5. 参照渡し (&) のメリット「変数に あだ名 をつける」機能。主な利点は以下の通り。複数の値を実質的に「返す」:C++の return は通常1つしか返せないが、void 関数の引数を参照渡しにすることで、複数の変数を直接書き換えられる。メモリと時間の節約:巨大な vector などを関数に渡す際、コピー（値渡し）を避けて高速化できる。コードの簡略化:深い階層の変数（table[i][j][k] など）に短いあだ名をつけて操作できる。